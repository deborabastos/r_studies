rsconnect::deployApp(
  appName = "meuApp",
  account = "meuuser"
)

# Se tiver dentro do Golem, usa a função deploy
deploy <- function() {
  rsconnect::deployApp(
    appName = "meuApp",
    account = "meuuser"
  )
}